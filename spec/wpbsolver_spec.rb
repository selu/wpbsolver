require "spec_helper"

describe WPBSolver do
  it "has a version number" do
    expect(WPBSolver::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
